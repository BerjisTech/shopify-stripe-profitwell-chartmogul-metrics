<%= form_with(model: blocker) do |form| %>
  <% if blocker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blocker.errors.count, "error") %> prohibited this blocker from being saved:</h2>

      <ul>
        <% blocker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_area :item %>
  </div>

  <div class="field">
    <%= form.label :added_by %>
    <%= form.text_field :added_by %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
