<%= form_with(model: slack) do |form| %>
  <% if slack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(slack.errors.count, "error") %> prohibited this slack from being saved:</h2>

      <ul>
        <% slack.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event %>
    <%= form.text_field :event %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
