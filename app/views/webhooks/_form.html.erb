<%= form_with(model: webhook) do |form| %>
  <% if webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webhook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
        <% webhook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :origin %>
    <%= form.text_area :origin %>
  </div>

  <div class="field">
    <%= form.label :app_id %>
    <%= form.number_field :app_id %>
  </div>

  <div class="field">
    <%= form.label :platform %>
    <%= form.text_field :platform %>
  </div>

  <div class="field">
    <%= form.label :slag %>
    <%= form.text_field :slag %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
